## Review: v1.0.3 — per-skill end-to-end test artifacts (commit de1f25c)

**Reviewer:** Hermes + review skill methodology
**Diff scope:** 6 new files, ~1100 lines

### Blocking (must fix before merge)

None.

### Non-blocking (nice to have)

- [scrape/examples/pypi-v...cker.py] **Filename is truncated by git status** — when list is too long, git shows `pypi-v...cker.py` instead of the full name. Workaround: rename to `pypi_check.py` or `pypi_scraper.py` so it fits.
  - File: scrape/examples/pypi-version-checker.py (truncated display only)
  - Issue: Naming convention; not functionally important
- [office-hours/brief-test-metasearch.md:5] **Spec positions it as part of "today"**
  - User context: this is real
  - Issue: Not a code issue, just noting these are 2026-07-23 dated artifacts. If a future reader is auditing "what was the state of thinking on 2026-07-23?", these are gold.
- [specs/SPEC-test-metasearch.md:6] **One MUST requirement isn't testable as-is**
  - "Embedding model pinned to a specific version in `requirements.txt`"
  - Issue: doesn't say what version, what threshold for "version drift". Could be a SHOULD/SHOULD-become-MUST.
  - Suggested fix: "MUST — embedding model pinned to `sentence-transformers==X.Y.Z`; version drift triggers a rebuild warning at next cron run"

### Questions

- [All artifacts] **Where do the test artifacts officially live?**
  - We've now shipped two rounds: v1.0.2 had 7 artifacts under each
    skill directory; v1.0.3 adds another 6. Are they "examples" or
    "audit log" or "deprecation graveyard"? Worth standardizing.
  - Suggestion: rename to `examples/` subdir per skill, or ship as
    a separate `audit/` directory at repo root.

### Praise

- **The `investigate` artifact is exemplary**: it shows the
  reproducible reproducer (the actual `python -m unittest discover`
  command), 5 hypotheses with results, root cause naming both
  compounding issues, and a regression test stub. Pattern for any
  future investigation.
- **The `qa` artifact caught a real CAPTCHA trip** for headless
  automation, which is exactly the kind of issue that humans never
  see but automation always hits. Good adversarial thinking.
- **The `scrape` artifact validates the prototype-then-codify pattern**
  on a clean, simple API (PyPI JSON). Shows the skill works for
  the easy case; if the user wants to scrape something harder later,
  they have a baseline.

### Checklist summary

- [x] Correctness — every artifact matches its skill's prescribed
  output template
- [x] Security — no secrets introduced; the qa artifact explicitly
  notes that uploading vault content to a cloud LLM would leak it
- [x] Performance — the scrape artifact times its codify phase;
  the per-skill test loop runs in seconds
- [x] Maintainability — artifacts have author + date + scope
- [x] Operability — the retro artifact lists next-week checkboxes
  that are testable
- [x] Documentation — every artifact includes its methodology provenance

### Verdict

**Approve.** v1.0.3 ships 6 useful test artifacts (and re-uses 1
already-shipped). The repetition of the artifact-per-skill
pattern will compound: future audits of the repo can compare
"what artifacts exist?" vs "what skills claim to work?", and
verify the ratio.

### Pre-merge sanity checks (none flagged)

- All 6 new files render correctly as Markdown
- No secrets in the diff (verified via grep)
- All paths use `references/` for upstream pointers, never
  embed them inline (consistent with skill format spec)
